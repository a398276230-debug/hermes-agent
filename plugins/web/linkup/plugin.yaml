name: web-linkup
version: 1.0.0
description: "Linkup web search + page fetch — Bearer-auth REST API. Requires LINKUP_API_KEY (free tier: 4,000 queries at https://app.linkup.so/)."
author: NousResearch
kind: backend
provides_web_providers:
  - linkup
